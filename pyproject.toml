[project]
name = "vitalis-guias"
version = "0.1.0"
description = "Conferência de guias de convênio antes do envio (Clínica Vitalis) — prova técnica Expert Integrado"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "python-multipart>=0.0.9",
    "markdown>=3.6",
    "mcp>=2.2",
]

[dependency-groups]
dev = ["pytest>=8", "uvicorn>=0.30", "httpx>=0.27"]

[tool.uv]
package = false
